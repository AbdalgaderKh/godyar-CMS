; Godyar — production PHP hardening (LiteSpeed reads .user.ini)
expose_php = Off
display_errors = Off
display_startup_errors = Off
log_errors = On

; Session hardening
session.cookie_secure = 1
session.cookie_httponly = 1
session.cookie_samesite = Strict
session.use_strict_mode = 1

; Optional: restrict session IDs to cookies only
session.use_only_cookies = 1
